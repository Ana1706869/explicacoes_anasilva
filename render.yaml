services:
  - type: web
    name: explicacoes-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm ci --omit=dev
    startCommand: node server.js
    autoDeploy: false
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: MONGO_URI
        sync: false
      - key: PUBLIC_BASE_URL
        sync: false
